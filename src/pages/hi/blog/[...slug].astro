---
import Page, { getStaticPaths as gsp } from '../../blog/[...slug].astro';
export const getStaticPaths = typeof gsp === 'function' ? gsp : undefined;
const props = Astro.props;
---
<Page {...props} />
