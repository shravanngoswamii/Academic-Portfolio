---
import Page, { getStaticPaths as gsp } from '../../tags/[tag].astro';
export const getStaticPaths = typeof gsp === 'function' ? gsp : undefined;
const props = Astro.props;
---
<Page {...props} />
